# 軌道鞅「大實體K關卡」假設驗證 — 設計文件 (Step 1)

- 日期: 2026-06-15
- 分支: feature/tdcc-whale-accumulation (本實驗會另開目錄,不動既有策略)
- 狀態: 設計已與 Tom 確認,待 spec review → 進實作計畫

## 0. 背景

軌道鞅(玩股網講師,台大經濟碩士,著《高勝率新手專用:三招縱橫股期權》)主打台指期**當沖三法**:

1. **關卡過破法** — 突破/跌破關鍵價位
2. **大紅K大黑K底頂部 停損停利法** — 用大實體K的實體邊緣當支撐壓力 + 停損停利
3. **滿足點預測法** — 用缺口/前段幅度推算目標價

帳號名「軌道**鞅**」取自兵法「詭道」:**上下影線是虛(詭),只有實體是真**。這是貫穿全系統的設計原則。

Tom 自陳「不知道他的具體做法跟實操」。因此本實驗不是「照抄規則」,而是**把核心原則寫成可量化、可統計檢定的東西,讓資料告訴我們地基成不成立**,再決定進出場。

## 1. 本步驟目標(可驗證的成功標準)

> **證明:日K「大實體K」的實體邊緣(紅底/黑頂),作為後續價格的支撐/壓力,效果顯著優於隨機價位。**

地基成立 → 才往上疊進出場規則(Step 2)與盤中多時間框架(Step 3)。
地基不成立 → 及早止損,不在沒驗證的假設上蓋策略。

**本步驟不做任何交易/損益模擬**,純假設檢定。

## 2. 核心設計原則(詭道)

- 所有計算**只用實體 `open`→`close`,完全丟棄上下影線 `high`/`low`**。
- 「碰到關卡」也以**實體**判定(收盤/開盤觸及),影線穿越不算。

## 3. 資料

- **Step A 主資料**: 大盤加權指數日線 OHLC。
  - 現成: `~/shioaji-trading/data/TWII_daily.csv`(yfinance `^TWII`,有完整 OHLC,508 根,**但只到 2026-01-30**)。
  - 前置: 刷新到當日(yfinance,免券商)。
- **已知待查**: 加權指數(1 月 ~32000)與軌道鞅截圖(6 月台指期 45000+)數字落差大。載資料時查清楚成因。
  - 假設檢定為 scale 無關 → 先用加權驗「原則」;**期貨日K(TXF)版當忠實度跟進(Step A')**。
- **盤中(Step 3 用,非本步驟)**: 沿用既有 `~/shioaji-trading/data/shioaji_source.py::ShioajiDataSource.fetch_kbars()` 拉歷史分K。回溯範圍待登入實測。

## 4. 量化定義

```
body(i)       = abs(close[i] - open[i])          # 不含影線
is_red(i)     = close[i] > open[i]
is_black(i)   = close[i] < open[i]

# 「大實體K」: 相對前 N 根中位數的倍數(把「比旁邊長很多」量化)
big_body(i)   = body(i) > median(body[i-N : i]) * k
              # 掃參數: N ∈ {10, 20, 40}, k ∈ {1.5, 2.0, 2.5, 3.0}

# 關卡(實體邊緣):
紅底 level    = open[i]   for 大紅K (is_red & big_body)   # 支撐
黑頂 level    = open[i]   for 大黑K (is_black & big_body)  # 壓力
```

備註: 紅K 實體下緣 = `min(open,close)=open`;黑K 實體上緣 = `max(open,close)=open`。兩者皆為開盤價,定義一致。

## 5. 假設檢定方法(對照組 A:隨機價位)

對每一條關卡 `L`(形成於第 `i` 根),觀察其後價格行為:

- **碰觸(touch)**: 之後某根 `j>i`,其實體(`open`/`close`)進入 `L ± ε` 區間(`ε` 以點數或 ATR 比例設定,列為參數)。
- **反應成功(react)**: 支撐線被由上而下碰觸後,於 `W` 根內反彈 ≥ `R`(點數或 %),且期間實體未決定性跌破 `L`;壓力線對稱定義。
- **指標**: 每組 `(N,k)` 的 touch 次數、react 成功率、平均反應幅度。

**對照組 A(隨機價位)**: 同期間隨機抽 M 組「假關卡」價位(數量、分佈對齊真關卡),計算其 react 成功率。

**結論判準**: 真關卡 react 率顯著高於假關卡(報告給出差異與簡單顯著性檢定),則地基成立。

> 對照組 B(隨機日期事件法)為後續可加項,本步驟先做 A。

## 6. 模組結構(避免「又一支散落大腳本」)

新目錄 `taiex-orbital-yang/`,模組化:

| 模組 | 職責 | 對外介面 |
|---|---|---|
| `data_loader.py` | 載入/刷新日線 OHLC | `load_daily(symbol, start, end) -> DataFrame` |
| `body_levels.py` | 計算 body、判定大實體K、產出紅底/黑頂關卡 | `detect_levels(df, N, k) -> list[Level]` |
| `hypothesis_test.py` | touch/react 統計 + 隨機價位對照 | `evaluate(df, levels, params) -> Stats` |
| `report.py` | 彙整各 (N,k) 結果輸出報告 | `render(results) -> 報告檔` |

## 7. 驗證(單元測試)

- `body_levels`:
  - **詭道測試 1**: 「開低收平盤」(開在低、收回平盤=大實體紅K)**必須**被判為大紅K。
  - **詭道測試 2**: 「長十字影線」(上下影線很長、實體極小)**不可**被判為大K。
  - 紅底=開盤價、黑頂=開盤價 的邊界計算。
- `hypothesis_test`: 用手工 K 線驗證 touch/react 判定、隨機對照可重現(固定種子)。

## 8. 階段路線圖(本 spec 只涵蓋 Step A)

- **Step A(本文件)**: 日K(加權)假設驗證 — 地基。
- **Step A'**: 改用期貨日K(TXF)重跑,確認忠實度。
- **Step 2**: 地基成立後疊進出場規則(碰線進場、停損、滿足點停利),產權益曲線。可讓回測搜尋最佳進出場。
- **Step 3**: 多時間框架(日K/60分K 定關卡 → 5分K 進場觀點),沿用 Shioaji 歷史分K。
- **Step 4**: 選擇權損益模型(需 Greeks + 選擇權歷史報價,資料另尋)。

## 9. 不做(YAGNI)

- 本步驟不碰選擇權、不碰盤中、不做損益模擬、不接即時 logger(歷史分K 可拉,無需等)。
- 不重構既有 `~/shioaji-trading/` 與 stock-verify 既有策略碼。

## 10. Step A 結果 (2026-06-15)

**資料**: 加權指數日線 506 根 (2024-01 ~ 2026-01, 強多頭, 指數近乎翻倍)。

**第一版 (均勻對照)**: 12/12 組合 edge < 0 (真 0.32~0.40 vs 隨 0.42~0.47)。
Opus code review 判定為**對照組偏誤**: 均勻隨機價位在多頭被上漲行情由下而上掃過(躺贏), 真實支撐被由上而下回測(嚴苛); 負值幾乎全在支撐側, 壓力側打平。非 bug, 結果可從原始檔重現。

**第二版 (Tom 決策 A: 方向匹配對照 + 百分比門檻 0.1%/0.5% + 按多空拆分)**: commit `e7d0b6d`
- **支撐 edge vs 方向匹配**: 仍全負, -0.02 ~ -0.12 (12/12)。
- **壓力 edge vs 方向匹配**: 約等於零, -0.01 ~ +0.04。
- 均勻對照(支撐 0.53~0.58) vs 方向匹配(0.39~0.48) → 證實舊偏誤真實存在、已被分離。

**結論**: 即使用公平的方向匹配對照, 大實體K的實體邊緣在**日線加權指數**上作為被動支撐/壓力, **沒有正向 edge**(支撐負、壓力中性)。詭道「日線被動關卡」假設, 在此單一強多頭樣本上**不成立**。

**未檢驗的退路 (非本步驟結論能否定)**:
- 期貨日K (TXF, 他真正看的) 未測 → Step A'。
- 單一 2 年強多頭樣本; 盤整/空頭區間未測; 樣本短 (506 根)。
- 被動「碰觸反應率」≠ 帶停損的方向性策略 (買支撐+緊停損, 賠率不對稱) → Step 2。
- 他真正的玩法是多時間框架 (日/60分定關卡 → 5分進場) + 選擇權, edge 可能在盤中執行而非日線關卡本身 → Step 3。
